import Map from '../../common/Map/Map'

const Representatives = ({representatives}) => {
    return <div>
        <Map mapContainerId={'representatives-map'}/>
    </div>
}

export default Representatives